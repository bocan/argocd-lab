grafana:
  ingress:
    enabled: true
    ingressClassName: traefik
    path: /

    # pathType is only for k8s >= 1.1=
    pathType: Prefix

    hosts:
      - grafana.funderburg.uk
  persistence:
    type: pvc
    enabled: true
